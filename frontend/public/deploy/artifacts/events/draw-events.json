{
  "VRFRequested": {
    "signature": "VRFRequested(uint256 indexed roundId, uint256 indexed requestId)",
    "description": "Emitted when VRF randomness is requested for a round",
    "indexed_fields": ["roundId", "requestId"],
    "fields": {
      "roundId": {
        "type": "uint256",
        "description": "ID of the round requesting VRF"
      },
      "requestId": {
        "type": "uint256",
        "description": "Chainlink VRF request ID"
      }
    }
  },
  "VRFFulfilled": {
    "signature": "VRFFulfilled(uint256 indexed roundId, uint256 indexed requestId, uint256[] randomWords)",
    "description": "Emitted when VRF randomness is fulfilled",
    "indexed_fields": ["roundId", "requestId"],
    "fields": {
      "roundId": {
        "type": "uint256",
        "description": "ID of the round receiving VRF fulfillment"
      },
      "requestId": {
        "type": "uint256",
        "description": "Chainlink VRF request ID"
      },
      "randomWords": {
        "type": "uint256[]",
        "description": "Array of random words from VRF"
      }
    }
  },
  "WinnersAssigned": {
    "signature": "WinnersAssigned(uint256 indexed roundId, address[] winners, uint8[] prizeTiers)",
    "description": "Emitted when winners are assigned after VRF fulfillment",
    "indexed_fields": ["roundId"],
    "fields": {
      "roundId": {
        "type": "uint256",
        "description": "ID of the round with assigned winners"
      },
      "winners": {
        "type": "address[]",
        "description": "Array of winner addresses"
      },
      "prizeTiers": {
        "type": "uint8[]",
        "description": "Array of prize tiers for each winner"
      }
    }
  },
  "PrizeDistributed": {
    "signature": "PrizeDistributed(uint256 indexed roundId, address indexed winner, uint8 prizeTier, uint256 assetId)",
    "description": "Emitted when a prize is distributed to a winner",
    "indexed_fields": ["roundId", "winner"],
    "fields": {
      "roundId": {
        "type": "uint256",
        "description": "ID of the round"
      },
      "winner": {
        "type": "address",
        "description": "Address of the winner"
      },
      "prizeTier": {
        "type": "uint8",
        "description": "Prize tier (1=Fake, 2=Kek, 3=Pepe)"
      },
      "assetId": {
        "type": "uint256",
        "description": "Emblem Vault asset ID"
      }
    }
  },
  "FeesDistributed": {
    "signature": "FeesDistributed(uint256 indexed roundId, address indexed creators, uint256 creatorsAmount, uint256 nextRoundAmount)",
    "description": "Emitted when fees are distributed after round completion",
    "indexed_fields": ["roundId", "creators"],
    "fields": {
      "roundId": {
        "type": "uint256",
        "description": "ID of the completed round"
      },
      "creators": {
        "type": "address",
        "description": "Address receiving creator fees"
      },
      "creatorsAmount": {
        "type": "uint256",
        "description": "Amount sent to creators (80%)"
      },
      "nextRoundAmount": {
        "type": "uint256",
        "description": "Amount retained for next round (20%)"
      }
    }
  }
}
