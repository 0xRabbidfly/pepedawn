{
  "WagerPlaced": {
    "signature": "WagerPlaced(address indexed wallet, uint256 indexed roundId, uint256 amount, uint256 tickets, uint256 effectiveWeight)",
    "description": "Emitted when a user places a wager in a round",
    "indexed_fields": ["wallet", "roundId"],
    "fields": {
      "wallet": {
        "type": "address",
        "description": "Address of the wallet placing the wager"
      },
      "roundId": {
        "type": "uint256",
        "description": "ID of the round the wager is placed in"
      },
      "amount": {
        "type": "uint256",
        "description": "Amount wagered in wei"
      },
      "tickets": {
        "type": "uint256",
        "description": "Number of tickets purchased"
      },
      "effectiveWeight": {
        "type": "uint256",
        "description": "Effective weight for the wager (includes proof multiplier if applicable)"
      }
    },
    "monitoring": {
      "alerts": [
        {
          "condition": "amount > 1 ether",
          "message": "Large wager detected"
        },
        {
          "condition": "tickets > 10",
          "message": "Invalid ticket count"
        }
      ],
      "metrics": [
        "total_wagers_per_round",
        "average_wager_amount",
        "tickets_distribution"
      ]
    }
  }
}
